wdir=/eng/ssb/pandokia_dev/pandokia


if [ `pwd` != $wdir ]
then
	echo wrong directory
	exit 1
fi
echo ok

for x in arzach bond cadeau herbert thor 
do
	echo XXXXX
	echo $x
	ssh $x "cd $wdir && rm -rf build && ./go iraf && ./go irafx && ./go irafdev"
	exit 0
done

echo XXXXX
echo arzach again - earlier python versions
ssh arzach "cd $wdir && rm -rf build && ./go irafdev 2.5.4 && ./go irafdev 2.6.5"

ssh tufnel 'cd pandokia_dev/pandokia && svn update && rm -rf build && sh go pyssg && sh go ssbdev'
